import React from "react";
import Header from "../components/Header";

const Projects = () => {
	return (
		<div className='flex flex-col bg-base h-screen'>
			<Header topic={`>> /`} content={`Projects`} />
			Projects
		</div>
	);
};

export default Projects;
