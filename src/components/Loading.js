import React from "react";

const Loading = () => {
	return (
		<div className='w-full h-screen flex flex-col items-center justify-center text-textPurple bg-base'>
			Loading...
		</div>
	);
};

export default Loading;
